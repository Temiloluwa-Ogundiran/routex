---
title: Collections
description: Initialize a hosted checkout session and return a RouteX checkout link.
openapi: POST /api/v1/initiate
playground: interactive
---

# Collections

Create a hosted checkout session and get back one RouteX checkout link. The customer chooses the payment method on the hosted page.

## Request

| Field | Required | Description |
| --- | --- | --- |
| `reference` | Yes | Unique merchant reference for this checkout. |
| `amount` | Yes | Amount in customer-facing currency units. Send `2500` for `NGN 2,500`. |
| `currency` | Yes | Currency code. This MVP supports `NGN`. |
| `customer.email` | Yes | Customer email address. |
| `customer.name` | No | Optional customer display name. |
| `gateway_code` | No | Optional gateway override. Use `pstk`, `fltw`, `kora`, or `isw`. |
| `redirect_url` | No | Optional customer return URL after payment. |
| `notification_url` | No | Optional merchant webhook URL for normalized RouteX events. |
| `narration` | No | Optional checkout narration. |
| `metadata` | No | Optional metadata object. |

## Example request

```json
{
  "reference": "ORD_1001",
  "amount": 2500,
  "currency": "NGN",
  "customer": {
    "email": "buyer@example.com",
    "name": "Ada Obi"
  },
  "redirect_url": "https://merchant.example.com/return",
  "notification_url": "https://merchant.example.com/webhook",
  "narration": "Invoice payment",
  "metadata": {
    "order_id": "ORD_1001"
  }
}
```

## Example response

```json
{
  "status": true,
  "message": "Charge created successfully",
  "reference": "ORD_1001",
  "checkout_url": "https://checkout.routex.xoroai.cloud/ORD_1001/20260325",
  "selected_gateway": "isw"
}
```

## Notes

- `gateway_code` is optional.
- When `gateway_code` is omitted, RouteX picks the gateway automatically.
- When `gateway_code` is present, RouteX uses that gateway if it is eligible.
- Manual override example: `{ "gateway_code": "isw" }`
- RouteX handles gateway-specific amount conversion for you.
- The hosted checkout page lets customers choose the payment method.
- If you send `notification_url`, RouteX uses it for merchant webhook delivery after provider confirmation.
